import React from 'react';

const NotMatch = () => {
    return (
        <div style={{textAlign: 'center', marginTop: '50px'}}>
            <h1 style={{fontStyle: 'bold',color: 'red'}}>404 Error</h1>
            <h3>This is not the web <br/>
            page you are loooking</h3>
        </div>
    );
};

export default NotMatch;